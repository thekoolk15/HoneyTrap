# HoneyTrap - No external dependencies required
# Runs entirely on the Python 3.7+ standard library
#
# Python version: 3.7+
#   python --version
#
# Docker (optional):
#   docker --version
#   docker-compose --version
#
# Optional extras for future enhancements:
#   pip install geoip2        # Map attacker IPs to countries
#   pip install requests      # Integrate with threat intel APIs
#   pip install rich          # Pretty terminal output
#   pip install flask         # Build a monitoring dashboard