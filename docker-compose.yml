version: '3.8'

services:
  honeytrap:
    build: .
    container_name: honeytrap
    ports:
      - "2222:2222"
      - "8022:8022"
      - "2022:2022"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped